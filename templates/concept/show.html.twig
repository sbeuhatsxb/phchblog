{% extends 'base.html.twig' %}

{% block title %}Concept{% endblock %}

{% block body %}
    <h1>Concept</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ concept.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ concept.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('concept_index') }}">back to list</a>

    <a href="{{ path('concept_edit', {'id': concept.id}) }}">edit</a>

    {{ include('concept/_delete_form.html.twig') }}
{% endblock %}
